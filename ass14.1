#include<stdio.h>
main()
{
	int i,n,j,k;
	printf("enter the value of n\n");
	scanf("%d",&n);
	for(i=1 ; i<=n ; i++)
	{
		for(j=1 ; j<=i ; j++)
		{
			printf("%d",j);
		}
		printf("\n");
	}
	for(i=1 ; i<=n-1 ; i++)
	{
	   for(k=i ; k<=n-1 ; k++)
		{
			printf("%d",k);
		}
		printf("\n");
	}
}
