#include<stdio.h>
main()
{
	int i,n,m;
	printf("enter the value of table you want to print\n");
	scanf("%d",&n);
	printf("enter the limit of the table\n");
	scanf("%d",&m);
	for(i=1 ; i<=m ; i++)
	{
		printf("\n%d X %d = %d",n,i,(n*i));
	}
}
