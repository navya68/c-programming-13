#include<stdio.h>
main()
{
	int i,n,j=0,k,r,sum=0;
	printf("enter the value of n\n");
	scanf("%d",&n);
	while(n!=0)
	{
		r=n%10;
		sum=sum+r;
		n=n/10;
	}
		while(sum!=0)
	{
		k=sum%10;
		j=j+k;
	    sum=sum/10;
	}
	   printf("sum = %d",j);
	
}
